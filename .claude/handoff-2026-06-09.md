# Session Handoff — 2026-06-09

## Accomplished (storefront = tissu-page)
- Fixed hero title gluing words ("Alittlecolour" → "A little colour every day").
- Fixed order-history page crash (orders list could be undefined).
- Profile page: removed "fast delivery auto-selected" note, fixed wishlist wording
  ("სურვილების სია ცარიელია"), added **in-place password change** (current + new) with a
  forgot-password link (auth.service.changePassword verifies current pw first).
- Necklace builder: picked **charms now show as photos in the cart**; reworded
  "ააწყვე შენი გემოვნებით" / "Build it your way" and "შენი შექმნილი ყელსაბამი" / "Your own necklace".
- Shop: mobile grid toggle now reads **1/2** (desktop keeps 2/4); home **"Shop all bags"**
  CTA now opens shop filtered to bags (`?category=bag`).
- CMS wiring (storefront reads tissu-agent CMS, falls back to built-in copy):
  hero (live before), **shop hero title**, home **products heading**, home **"how it works"
  title/CTA**, **FAQ** page. Footer/contact already wired.
- **SEO**: per-product title/description/OG photo; JSON-LD (Organization + WebSite sitewide,
  Product on product pages); canonical + ka/en hreflang; robots.txt + sitemap.xml fallback → tissu.ge.
- Social share card (Open Graph): English hero banner at /og-image.jpg (Georgian version was made
  then reverted per request — English kept).

## Accomplished (admin = tissu-agent, separate repo at ~/Desktop/tissu agent)
- Site CMS tab: pre-filled all wired fields with current GE/EN text; added missing hero
  2nd-line field; added a **"კონტაქტი" page editor**; server.py allows contact "details" section.
- Committed locally + pushed to branch `feature/photo-analysis` (production branch for the agent).

## Current State
- Storefront: all work committed. **5 commits pending merge** on PR branch **`seo-search-ready`**
  (scroll fix, shop 1/2, bags filter, SEO, English banner). User merges PRs manually on GitHub
  (gh CLI absent + GitHub MCP auth fails → push branch, user clicks Merge).
- Cloudflare now fronts tissu.ge (proxied/orange, SSL Full strict). Web Analytics enabled
  ("Enable, excluding EU" — could switch to "Enable" for all visitors).

## Next Steps (tomorrow)
1. User to merge the `seo-search-ready` PR (deploys everything pending). No Cloudflare purge needed
   (banner unchanged = English).
2. **Google Search Console** — register tissu.ge + submit sitemap.xml (code alone won't get it indexed).
3. **Cloudflare Access** for /admin (user chose this) — gate tissu.ge/admin behind email login so the
   public can't reach it. Note: storefront /admin is already password-protected (middleware + signed cookie).

## Notes
- Deploys: storefront from `main`; pushing to main directly is auto-blocked → use a branch + PR.
- Verify Railway deploy by checking the live URL (e.g., curl content-type of /og-image.jpg).
- Cloudflare caches static asset URLs aggressively — purge after replacing a same-named file.
